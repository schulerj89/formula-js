# Agent Notes

- Keep this repo mobile-first and performance-measured.
- Preserve user changes; avoid broad rewrites.
- Use procedural fallbacks before external assets.
- Never commit API keys or generated secrets.
- When a domain decision is uncertain, use the relevant expert/subagent and record the decision in `docs/decisions/`.
- Radio team audio should use its own radio-processed voice ID, not Arthur or Mags announcer voices.
- Keep the title page decluttered; favor playable entry points and secondary links over dense text.
- Check generated GLB/model assets in the isolated asset inspector before integrating them into the main game.
